pre_bump_hooks = [
  "sed -i -E 's/^version:.*/version: {{version}}/' pubspec.yaml",
]

[changelog]
path = "CHANGELOG.md"
template = "remote"
remote = "gitlab.com"
owner = "HttpAnimations"
repository = "er"

[bump_profiles.ci]
pre_bump_hooks = [
  "sed -i -E 's/^version:.*/version: {{version}}/' pubspec.yaml",
]
